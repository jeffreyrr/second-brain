# raw/ — your sources (immutable)

Drop source files here (articles, papers, images, data), then tell Claude
`ingest <filename>` or `ingest all`. Claude reads these but never edits or deletes
them. Descriptive filenames become the wiki page slug.
